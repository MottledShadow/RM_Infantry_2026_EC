/**
 ******************************************************************************
 * @file    bsp_gpio.h
 * @brief   GPIO 底层驱动头文件（BSP 层）
 * @note    层级说明：
 *            本文件属于【BSP 层】，封装 HAL GPIO 读写原语。
 *            上层（控制层）通过语义化函数名读取引脚状态，
 *            不直接接触 GPIOx / GPIO_PIN_x 等 HAL 细节。
 *
 *          硬件映射（TODO: 如有硬件改动请在此处同步修改）：
 *            弹丸检测光电门 → GPIOB PIN_12，低电平有效（遮光触发）
 *
 * @version 1.0
 * @date    2026-4-5
 * @author  黄仲华
 ******************************************************************************
 */

#ifndef BSP_GPIO_H
#define BSP_GPIO_H

#include "main.h"
#include <stdint.h>

/* ========================================================================== */
/*                          引脚硬件映射（魔数集中管理）                         */
/* ========================================================================== */

#define SHOT_DETECT_GPIO_PORT   GPIOB        /**< 弹丸检测光电门 GPIO 端口 */
#define SHOT_DETECT_GPIO_PIN    GPIO_PIN_12  /**< 弹丸检测光电门 GPIO 引脚 */

/* ========================================================================== */
/*                              函数声明                                        */
/* ========================================================================== */

/**
 * @brief  读取弹丸检测光电门的当前遮光状态
 * @note   电平极性说明：光电门遮光（弹丸通过）时 GPIO 为低电平（GPIO_PIN_RESET），
 *         本函数已做极性转换，返回值 1 表示"有弹丸"，0 表示"无弹丸"。
 *         与 key_scan 中 KEY_LEVEL_PRESSED = 1 的约定一致。
 * @retval 1: 光电门被遮挡（弹丸在通道中）
 *         0: 光电门通畅（无弹丸）
 */
uint8_t BSP_GPIO_ReadShotDetect(void);

#endif /* BSP_GPIO_H */
